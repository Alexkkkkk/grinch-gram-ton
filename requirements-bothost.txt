# ── Bothost-специфичные зависимости ───────────────────────────────────────────
# Отличия от requirements.txt:
#   - xgboost удалён  (131.7 МБ — переполнял диск при сборке; при LOW_MEMORY_MODE=1
#                      даже не импортируется, ai_engine.py обходится без него)
#   - ccxt удалён     (5.8 МБ — нужен только для CEX-режима; exchange.py импортирует
#                      его лениво внутри try/except, падения не будет)
#   - eventlet удалён (0.4 МБ — SocketIO работает в async_mode="threading",
#                      monkey_patch не вызывается, пакет бесполезен)
# Итого экономия при сборке: ~138 МБ

# ── Core Web ──────────────────────────────────────────────────────────────────
flask==3.1.3
flask-socketio==5.6.1
flask-sqlalchemy==3.1.1
flask-compress==1.24
gunicorn==26.0.0
orjson

# ── Database ──────────────────────────────────────────────────────────────────
psycopg2-binary==2.9.12
sqlalchemy==2.0.52

# ── AI / ML ───────────────────────────────────────────────────────────────────
# xgboost намеренно исключён — см. комментарий выше
numpy==1.26.4
pandas==2.3.3
scikit-learn==1.9.0
joblib

# ── Technical Analysis ────────────────────────────────────────────────────────
ta==0.11.0

# ── TON Blockchain ────────────────────────────────────────────────────────────
pytoniq==0.1.43
pytoniq-core
dedust==1.1.4
cryptography

# ── HTTP / Async ──────────────────────────────────────────────────────────────
requests==2.34.2
aiohttp==3.14.3

# ── Misc ──────────────────────────────────────────────────────────────────────
python-dotenv==1.2.3
python-telegram-bot==21.11.1
openai
email-validator
apscheduler==3.11.3
psutil

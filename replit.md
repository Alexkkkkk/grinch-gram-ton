# QuantumBrain — TON/GRINCH Trading Bot

## Обзор проекта

Автоматический торговый бот для пары GRINCH/TON на блокчейне TON через DEX DeDust.  
Включает веб-дашборд (Flask + SocketIO), AI-движок (6 моделей sklearn/XGBoost), мультипользовательскую платформу (TonConnect) и систему мониторинга кошельков.

## Стек

- **Backend:** Python 3, Flask, Flask-SocketIO, Gunicorn, Eventlet
- **AI:** scikit-learn (RF, ET, GB, HGB), XGBoost, MLP — QuantumBrain v4
- **Блокчейн:** pytoniq, dedust SDK, TonCenter API
- **БД:** PostgreSQL (основная) + JSON-файлы (резервный fallback)
- **Данные:** DexScreener, GeckoTerminal, CoinGecko

## Как запустить

```bash
python3 main.py
```

Или через workflow **Start application** (порт 5000).

## Ключевые переменные окружения

| Переменная | Описание |
|---|---|
| `SESSION_SECRET` | Секрет Flask-сессий |
| `TON_MNEMONIC` | Мнемоника кошелька TON (для реальной торговли) |
| `DATABASE_URL` | PostgreSQL строка подключения (Replit, fallback) |
| `EXTERNAL_DATABASE_URL` | Строка подключения к внешней PostgreSQL (приоритетна над `DATABASE_URL`) |
| `ADMIN_USERNAME` | Логин для входа в дашборд |
| `ADMIN_PASSWORD` | Пароль для входа в дашборд |
| `GROQ_API_KEY` | Ключ Groq AI-советника (опционально, можно задать через дашборд) |

Без `TON_MNEMONIC` бот работает в **демо-режиме** (без реальных сделок).

## Статус на Replit

✅ **Workflow запущен и работает (25.07.2026)**. Зависимости установлены через Replit package manager. Приложение поднимается на порту 5000, дашборд доступен в preview-панели.

### Исправления диагностики VPS

- AI-свечи в торговом цикле явно запрашиваются в валюте токена (`token/base`), как и свечи дашборда.
- Автосоветник Groq ограничен минимумом 150 минут между запусками и использует компактный снимок данных.
- Резервы DeDust-пула кэшируются на 45 секунд, а ошибка/429 TonAPI сохраняет последний рабочий курс.
- Ошибка `KeyError` в LiteClient обрабатывается повторным запуском провайдера с очисткой неудачного экземпляра.

Эти изменения внесены в Replit-копию. Боевой VPS не изменяется автоматически без безопасно добавленных SSH-учётных данных.

- **Replit-инстанс** использует СВОЮ отдельную внешнюю БД `bothost_db_bf4fb06bbd60` (node1.pghost.ru:15529) — она уже была настроена отдельно от VPS-бота (`bothost_db_261deccc4eba` на node1.pghost.ru:15873), общих таблиц больше нет.
- **`TON_MNEMONIC` НЕ задан на Replit** и `DEMO_MODE=true` — Replit-инстанс работает исключительно в демо-режиме, без реального кошелька и без реальных сделок. Дашборд отображает бейдж «ДЕМО».
- Боевой бот с реальными деньгами остаётся ТОЛЬКО на VPS (2.27.25.126, контейнер `bot-bot-1`). Не добавляйте `TON_MNEMONIC` на Replit, если не собираетесь сознательно превратить этот инстанс в боевой — иначе будет две реальные торговые системы на одном кошельке.
- `ADMIN_PASSWORD` для дашборда Replit ещё не задан (только `ADMIN_USERNAME`) — пока пароль не добавлен, вход в панель НЕ требуется (открыт всем). Рекомендуется задать `ADMIN_PASSWORD` через Secrets и перезапустить workflow.

⚠️ В переписке и загруженных файлах несколько раз засветился пароль root от VPS в открытом виде — агент его не хранит, но пользователю стоит сменить пароль root и пароль от панели vm.senko.digital.

### Найдено при обследовании VPS-бота (15.07.2026)
На VPS реальный кошелёк держит GRINCH, но открытая позиция бота в `bot_open_trades` была пустой (0 записей) — учёт истории сделок/equity в БД, судя по всему, прервался ~13.07.2026 (последняя точка equity от 13.07, хотя бот продолжал реально торговать: TON изменился с 3.6 до 268, GRINCH с 857k до 227k без единой записи в `bot_trades`). Это отдельная задача восстановления учёта на VPS, не связанная с Replit-инстансом — см. переписку с пользователем.

## Структура

- `main.py` — точка входа
- `app.py` — Flask-приложение, роуты, SocketIO-события
- `trader.py` — основной торговый движок
- `ai_engine.py` — QuantumBrain AI (обучение и предсказания)
- `dedust_client.py` — клиент DeDust DEX (свапы TON↔GRINCH)
- `config.py` — все настраиваемые параметры
- `db_store.py` — работа с PostgreSQL (7 таблиц)
- `experience_manager.py` — AI-адаптация параметров по опыту
- `wallet_tracker.py` — мониторинг кошельков умных денег
- `deposit_monitor.py` — мониторинг депозитов пользователей
- `templates/` — HTML-шаблоны дашборда
- `static/` — JS/CSS ресурсы

## Пользовательские настройки

- Язык интерфейса: **русский**
- Язык общения с агентом: **русский** (всегда отвечать на русском)
- **Пользователь работает ТОЛЬКО с VPS (2.27.25.126, контейнер `bot-bot-1`).** Replit используется только как редактор кода. Все правки деплоить на VPS через `scp` + `docker cp`. Replit-превью не используется.
- **В начале каждого ответа** — перечитать `.agents/memory/work-in-progress.md`, вывести актуальный список открытых задач и обновить файл по результатам сессии.

## Правило ведения задач при разработке

При написании или изменении кода агент одновременно ведёт подробную задачу, чтобы
следующая сессия могла продолжить работу без повторного расследования:

1. Перед началом зафиксировать цель, контекст, ограничения, затронутые компоненты,
   порядок действий и критерии готовности.
2. Во время работы отмечать выполненные этапы, технические решения, обнаруженные
   проблемы и способы их решения.
3. После каждого значимого этапа обновлять `.agents/memory/work-in-progress.md`.
4. В конце указывать изменённые файлы, поведение до и после, результаты проверок,
   риски, незавершённые пункты и точные следующие шаги.
5. Не отмечать задачу завершённой, пока не проверены все требования пользователя.
6. Не сохранять в задачах, памяти или документации секреты, пароли, токены,
   мнемоники и персональные данные.
